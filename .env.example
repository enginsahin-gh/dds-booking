# Supabase
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Netlify Function env vars (set in Netlify dashboard)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# SMTP_HOST=mail.ensalabs.nl
# SMTP_PORT=587
# SMTP_USER=boekingen@ensalabs.nl
# SMTP_PASS=your-smtp-password
# EMAIL_SECRET=shared-secret-for-function-auth (REQUIRED — email function rejects requests without it)
